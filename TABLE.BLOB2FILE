CREATE TABLE "BLOB2FILE" 
   (	"CONTENT" BLOB, 
	"NAME" VARCHAR2(30)
   ) 
 LOB ("CONTENT") STORE AS SECUREFILE "LOB_CONTENT"(ENABLE STORAGE IN ROW CHUNK 8192
  NOCACHE LOGGING  COMPRESS MEDIUM  KEEP_DUPLICATES ) ;
CREATE UNIQUE INDEX "SYS_IL0000297787C00001$$" ON "BLOB2FILE" (
  ;