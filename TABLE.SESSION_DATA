CREATE TABLE "SESSION_DATA" 
   (	"SESSION_ID" NUMBER, 
	"TIMEZONE" VARCHAR2(30), 
	"MAXTOUCHPOINTS" NUMBER, 
	"IP_ADDRESS" VARCHAR2(45), 
	"SUPPORTS_POPOVER" VARCHAR2(5), 
	"CREATED_DATE" TIMESTAMP (6) WITH TIME ZONE DEFAULT current_timestamp, 
	"SUPPORTS_EYEDROPPER" VARCHAR2(5), 
	 CONSTRAINT "PK_SESSION_DATA" PRIMARY KEY ("SESSION_ID")
  USING INDEX  ENABLE
   ) ;
CREATE UNIQUE INDEX "PK_SESSION_DATA" ON "SESSION_DATA" ("SESSION_ID") 
  ;