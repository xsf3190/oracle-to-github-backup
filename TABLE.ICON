CREATE TABLE "ICON" 
   (	"ID" VARCHAR2(30), 
	"VIEWBOX" VARCHAR2(20), 
	"SVG" CLOB, 
	 CONSTRAINT "PK_ICON" PRIMARY KEY ("ID")
  USING INDEX  ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_ICON" ON "ICON" ("ID") 
  ;