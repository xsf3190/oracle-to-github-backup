CREATE TABLE "ICON" 
   (	"ID" VARCHAR2(30), 
	"VIEWBOX" VARCHAR2(20), 
	"SVG" CLOB, 
	"PRESERVEASPECTRATIO" VARCHAR2(15), 
	"TITLE" VARCHAR2(100), 
	 CONSTRAINT "PK_ICON" PRIMARY KEY ("ID")
  USING INDEX  ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_ICON" ON "ICON" ("ID") 
  ;