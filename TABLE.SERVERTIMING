CREATE TABLE "SERVERTIMING" 
   (	"PERFORMANCE_ID" NUMBER, 
	"SEQ" NUMBER, 
	"NAME" VARCHAR2(30), 
	"DURATION" NUMBER, 
	"DESCRIPTION" VARCHAR2(200), 
	 CONSTRAINT "PK_SERVERTIMING" PRIMARY KEY ("PERFORMANCE_ID", "SEQ")
  USING INDEX  ENABLE
   ) ;
CREATE UNIQUE INDEX "PK_SERVERTIMING" ON "SERVERTIMING" ("PERFORMANCE_ID", "SEQ") 
  ;