CREATE TABLE "ACCESS_TOKEN" 
   (	"TOKEN" VARCHAR2(64), 
	"USER_ID" NUMBER, 
	"CREATED_DATE" TIMESTAMP (6) WITH TIME ZONE DEFAULT current_timestamp, 
	 CONSTRAINT "PK_ACCESS_TOKEN" PRIMARY KEY ("TOKEN")
  USING INDEX  ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_ACCESS_TOKEN" ON "ACCESS_TOKEN" ("TOKEN") 
  ;