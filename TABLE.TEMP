CREATE TABLE "TEMP" 
   (	"JSON_DATA" CLOB, 
	 CONSTRAINT "ENSURE_JSON" CHECK (json_data IS JSON (STRICT)) ENABLE
   ) ;

  ALTER TABLE "TEMP" ADD CONSTRAINT "ENSURE_JSON" CHECK (json_data IS JSON (STRICT)) ENABLE;