CREATE TABLE "IP_LOOKUP" 
   (	"IP" VARCHAR2(45), 
	"CITY" VARCHAR2(30), 
	"COUNTRY_CODE" VARCHAR2(2), 
	"POSTAL" VARCHAR2(10), 
	"ERROR_CODE" NUMBER, 
	"ERROR_MESSAGE" VARCHAR2(100), 
	"CREATED_DATE" TIMESTAMP (6) WITH TIME ZONE DEFAULT current_timestamp, 
	 CONSTRAINT "PK_IP_LOOKUP" PRIMARY KEY ("IP")
  USING INDEX  ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_IP_LOOKUP" ON "IP_LOOKUP" ("IP") 
  ;