CREATE TABLE "ES_MODULES_FONTS" 
   (	"FONT_FILE_NAME" VARCHAR2(50), 
	"SHA1" VARCHAR2(40), 
	 CONSTRAINT "PK_ES_MODULES_FONTS" PRIMARY KEY ("FONT_FILE_NAME")
  USING INDEX  ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_ES_MODULES_FONTS" ON "ES_MODULES_FONTS" ("FONT_FILE_NAME") 
  ;