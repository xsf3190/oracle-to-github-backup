CREATE TABLE "WEBSITE_ICON" 
   (	"WEBSITE_ID" NUMBER NOT NULL ENABLE, 
	"ICON_ID" VARCHAR2(30), 
	 CONSTRAINT "PK_WEBSITE_ICON" PRIMARY KEY ("WEBSITE_ID", "ICON_ID")
  USING INDEX  ENABLE, 
	 CONSTRAINT "FK_WEBSITE_ICON" FOREIGN KEY ("WEBSITE_ID")
	  REFERENCES "WEBSITE" ("ID") ON DELETE CASCADE ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_WEBSITE_ICON" ON "WEBSITE_ICON" ("WEBSITE_ID", "ICON_ID") 
  ;

  ALTER TABLE "WEBSITE_ICON" MODIFY ("WEBSITE_ID" NOT NULL ENABLE);