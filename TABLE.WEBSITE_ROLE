CREATE TABLE "WEBSITE_ROLE" 
   (	"WEBSITE_ID" NUMBER, 
	"ROLE_ID" NUMBER, 
	"CREATED_DATE" TIMESTAMP (6) WITH TIME ZONE, 
	 CONSTRAINT "PK_WEBSITE_ROLE" PRIMARY KEY ("ROLE_ID", "WEBSITE_ID")
  USING INDEX  ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_WEBSITE_ROLE" ON "WEBSITE_ROLE" ("ROLE_ID", "WEBSITE_ID") 
  ;