CREATE TABLE "ES_MODULE" 
   (	"MODULE_NAME" VARCHAR2(50), 
	"HASH_VALUE" VARCHAR2(97), 
	"LAST_UPLOADED" DATE DEFAULT current_date, 
	"LAST_UPDATED" DATE, 
	"HOST_NAME" VARCHAR2(100), 
	"SHA1" VARCHAR2(40), 
	 CONSTRAINT "PK_ES_MODULE" PRIMARY KEY ("MODULE_NAME")
  USING INDEX  ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_ES_MODULE" ON "ES_MODULE" ("MODULE_NAME") 
  ;