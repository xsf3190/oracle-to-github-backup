CREATE TABLE "ROLE" 
   (	"ID" NUMBER, 
	"NAME" VARCHAR2(10), 
	 CONSTRAINT "PK_ROLE" PRIMARY KEY ("ID")
  USING INDEX  ENABLE, 
	 CONSTRAINT "ROLE_CHECK" CHECK (name in ('admin','owner','visitor')) ENABLE
   ) ;

  CREATE UNIQUE INDEX "PK_ROLE" ON "ROLE" ("ID") 
  ;

  ALTER TABLE "ROLE" ADD CONSTRAINT "ROLE_CHECK" CHECK (name in ('admin','owner','visitor')) ENABLE;