CREATE TABLE "CORE_WEB_VITALS" 
   (	"ID" NUMBER, 
	"NAME" VARCHAR2(20), 
	"VALUE" NUMBER, 
	"RATING" VARCHAR2(20), 
	"LOG_DATE" TIMESTAMP (6) WITH TIME ZONE DEFAULT current_timestamp, 
	"IP_ADDRESS" VARCHAR2(45), 
	"SESSION_ID" NUMBER, 
	"PAGE_ID" NUMBER
   ) ;