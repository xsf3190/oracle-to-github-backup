CREATE TABLE "WEBSITE_FONT" 
   (	"NAME" VARCHAR2(30), 
	"PARAMETERS" VARCHAR2(30), 
	"FALLBACK" VARCHAR2(10), 
	 CONSTRAINT "PK_WEBSITE_FONT" PRIMARY KEY ("NAME")
  USING INDEX  ENABLE
   ) ;
CREATE UNIQUE INDEX "PK_WEBSITE_FONT" ON "WEBSITE_FONT" ("NAME") 
  ;